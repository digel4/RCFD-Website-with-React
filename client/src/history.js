import { createBrowserHistory } from 'history'; 
// history is installed via react-router-dom. it's technically a separate library but react-router depends on it

export default createBrowserHistory();